# Build artefacts
main.js
main.js.map
*.tsbuildinfo
dist/

# WASM artefact — produced by the upstream `aozora-wasm` crate's
# `wasm-pack build --target web`. Treated as a vendored binary in
# release tags rather than committed to main. ADR-0001.
aozora.wasm

# Node / bun
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/
bun-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Local env overrides
.env
.env.local

# Test artefacts
coverage/
.tmp-*

# macOS junk in WSL mounts
:2e*
